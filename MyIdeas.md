Do not overmanipulate the code
Deeply analyse my codebase, and investigate in all my files and folders to know my codebase, fully and deeply
Resolve issues

recheck all my requests in this chat and report if they were done successfully


Do not overmanipulate the code
add the size of each file at the first part of its name, but keep this font smaller with kb, mb, gb, etc.


Do not over manipulate the code
Add an option in the settings and move finding and filtering option for web components in settings, using a check box to find all js, objects, images, and ...of a web page. rename current "Download All File Types" to "Download Major File Types" and modify its filtering option and name this new option to "Download All File Types" and keep it disabled by default. Set/modify the filter buttons, accordingly


make the rescan button smaller to fit sort, files mode, and rescan button in a row
--------------------------------------
Do not overmanipulate the code
always show sort bar, and add a switch to toggle between Media and files mode of the extension, and a button to rescan the files in the page,  and a check box to only show main extension files like zips (zip, iso, rar, 7z, etc), media (audios and videos), os based only and filter others out

Do not over manipulate the code
the filtering had two modes: default: for media (All/Videos/Audio/Other), and when Download all file types selected: (All/Media/Zips/OS Based/Others). I can't remind exact filter names. see them in the code and correct this filter showing based on their settings

Do not overmanipulate the code.
I want the hover icon button in the webpage to be shown only for media (Audio/Video links). keep the youtube as is. add a check box in settings to toggle these popups enable/disabled, and enabled by default

recheck all my requests in this chat and report if they were done successfully

Do not overmanipulate the code
the files detection is not functioning fully again. Resolve its functioning it

